describe('Address Dal - fromDto', () => {

    it('should return an AddressDal object with the correct properties', () => {

    });

    it('should return empty array when no data is provided', () => {
        
    });

});